classdef Rect < PsychHandle
    properties
        left
        right
        top
        bottom
    end

    methods
        function out = Rect(varargin)
        % Specify either center + width + height or
        % left + right + top + bottom
        end
    end
end
